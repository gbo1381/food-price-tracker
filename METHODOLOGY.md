# מעקב מחירי מזון יומי — אינדיקציה מקדימה למדד המחירים לצרכן

## מטרה
דגימה יומית של סל מוצרים מייצג לפי מדד המחירים לצרכן, לקבלת אינדיקציה מוקדמת לסעיף המזון
ולסעיף פירות וירקות של המדד בחודש הקרוב.

## מקור הנתונים
קבצי שקיפות המחירים היומיים (חוק שקיפות המחירים 2014), ארבע רשתות, סניף קבוע לכל רשת:

| רשת | פלטפורמה | סניף שנדגם |
|---|---|---|
| שופרסל | prices.shufersal.co.il (Azure) | 290 — דיל פ"ת יכין סנטר |
| רמי לוי | url.publishedprices.co.il (RamiLevi) | 55 |
| טיב טעם | url.publishedprices.co.il (TivTaam) | 14 |
| קרפור | prices.carrefour.co.il (u-code) | 9 |

כל רשת מפרסמת מדי יום קובץ `PriceFull` (כל המחירים בסניף) וקובץ `PromoFull` (כל המבצעים).

## מחיר
מחיר בפועל = מינימום בין מחיר המדף (`ItemPrice`) למבצע **פתוח לכל צרכן** — בדיוק כפי שהלמ"ס רושמת.
מבצע נחשב פתוח לכל אם: מועדון = "0 - כלל הלקוחות" (או ריק), אינו קופון, אינו התניית סל/מינימום קנייה,
אינו מתנה, וכמות מזכה ≤ 3 יח'. מבצעי מועדון/קופון נדחים.

## הסל
- **מזון ארוז (29 תת-סעיפים):** לכל תת-סעיף 2–3 ברקודים של מותגים לאומיים המשותפים לרשתות
  (אותו ברקוד בכל רשת → השוואה like-for-like). כיסוי 114.2‰ מתוך 148.52‰ של סעיף המזון.
  לא נדגמים: ארוחות מחוץ לבית/בעבודה/קיוסקים (31.7‰, אינם קמעונאות) ובשר כבש/חזיר (2.65‰, כמעט אין
  כברקוד ארוז — בעיקר שקיל).
- **פירות וירקות טריים (12 פריטים):** נדגמים כפריטים שקילים (₪/ק"ג) — 6 ירקות + 6 פירות.
  מעובדים/קפואים (120046/120047) אינם בסל היומי בשלב זה.
- הסל נבנה אוטומטית מהקטלוגים המלאים (`collector/build_basket.py`) וניתן להרחבה.

## שיטת המדד (אגרגט אלמנטרי, ברוח הלמ"ס)
1. **מוצר:** יחס מחיר מול מחירו ביום הבסיס (היום הראשון בו נצפה) — כניסת מוצר חדש אינה יוצרת קפיצה.
2. **תת-סעיף לרשת:** ממוצע גאומטרי (Jevons) של יחסי המוצרים.
3. **תת-סעיף בין רשתות:** ממוצע גאומטרי (משקל שווה לרשתות).
4. **סל:** ממוצע גאומטרי משוקלל במשקולות הלמ"ס 2025. מדד מזון (ללא פו"ר) ומדד פו"ר בנפרד, בסיס 100.

**פרשנות:** השינוי החודשי הממוצע במדד היומי (גיליון "השוואה חודשית") הוא האינדיקציה למדד של אותו חודש.

## מגבלות ידועות
- הסל עוקב אחר קבוצה קבועה של מוצרים; אות ה-m/m תקף גם אם רמת המחיר אינה זהה לרמת הלמ"ס.
- בשר בקר/עוף/דגים נדגמים כמוצרים ארוזים ולא כנתחי דלפק שקילים (הלמ"ס מצטטת בעיקר שקיל); ה-m/m
  צפוי לנוע יחד אך אינו זהה.
- פו"ר: קיים כבר מודל נאוקאסט על מחירי משרד החקלאות השבועיים (מתאם 0.91 למדד). הדגימה היומית
  משלימה אותו ותיבחן מולו אחרי כמה חודשי חפיפה.

## אוטומציה
GitHub Actions (`.github/workflows/collect.yml`) רץ יומית ב-04:40 UTC (07:40 שעון ישראל):
מוריד את הקבצים, מחשב את הסל, מוסיף שורות ל-`data/prices/YYYY-MM.csv`, ובונה מחדש את
`data/workbook/מעקב מחירי מזון.xlsx`. הכל נשמר בריפו הפרטי; אין תלות במחשב אישי.

## מבנה הריפו
```
collector/  sources.py (מתאמי רשתות) · collect.py (איסוף) · build_basket.py · build_index.py
            config.json · weights.csv · basket.csv · fv_products.csv
data/       prices/ (תצפיות יומיות) · catalog/ (קטלוגים מלאים) · workbook/ (האקסל) · index_daily.csv
```
